# Netlify site: https://captain.rapido.neighshopglobal.com
# Base directory in UI: new-version/frontend/captain

[build]
  command = "npm ci --prefix .. && npm run build:captain --prefix .."
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
